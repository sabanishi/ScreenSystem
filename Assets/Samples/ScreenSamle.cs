using System.Collections;
using System.Collections.Generic;
using Cysharp.Threading.Tasks;
using UnityEngine;

public class ScreenSamle : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        UniTask.Void(async () =>
        {

        });
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
